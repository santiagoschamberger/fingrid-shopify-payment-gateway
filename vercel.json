{
  "buildCommand": "npm run build",
  "outputDirectory": "build/client",
  "env": {
    "DATABASE_URL": "file:/tmp/prod.db"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}